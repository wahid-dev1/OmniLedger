# Database Configuration
# NOTE: This DATABASE_URL is ONLY for Prisma schema generation and development
# Actual database connections are configured at runtime per user via the application UI
# You can leave this as-is for local development with SQLite

# For SQLite (default - used for schema generation only)
DATABASE_URL="file:./data/omniledger.db"

# Application
NODE_ENV=development
